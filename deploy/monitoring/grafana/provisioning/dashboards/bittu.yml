apiVersion: 1

providers:
  - name: bittu
    orgId: 1
    folder: Bittu
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
